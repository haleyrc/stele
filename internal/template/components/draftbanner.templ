package components

// DraftBanner renders a warning banner indicating draft content.
templ DraftBanner() {
	<div class="mb-4 p-4 bg-yellow-50 border-l-4 border-yellow-400 text-yellow-800">
		<p class="font-medium">⚠️ This is a draft post</p>
		<p class="text-sm">This post is only visible in development mode and will not appear in production builds.</p>
	</div>
}
